[package]
name = "alertthread-store"
description = "Persistence layer for alertthread: the StateStore trait and its SQLite and PostgreSQL backends"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[features]
# Backend selection (ROADMAP Phase 2). Declared now, empty, so `just test-pg`
# and the CI matrix resolve against real feature names rather than being
# retrofitted later. Phase 2 attaches the sqlx drivers and migrations to them.
default = ["sqlite"]
sqlite = []
postgres = []

[dependencies]
alertthread-core.workspace = true

[lints]
workspace = true
